<h1>Tweets#index</h1>
<div>
    <%= form_for(@tweet) do |f|%>
      <%= f.text_area :body %>
      <%= f.submit %>
    <% end %>

</div>
<div>
    <% @tweets.each do |twwet|%>
     <div>
      <%= tweet.body %>
     </div>
    <% end%>
</div>